import 'package:dkit/src/kernel/context.dart';

///路由调度器
class Dispatcher
{
    ///路由收集器
    final Context _context;

    ///初始化路由调度器
    Dispatcher(this._context);

    ///路由调度
    void dispatch()
    {
//        _context.html('Hello,world!!!');
    }
}
